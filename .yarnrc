# Skip platform checks for optional dependencies (e.g. sharp's platform-specific bindings)
# Fixes Vercel build on Linux x64 when lockfile contains darwin-arm64 entries from local Mac dev
ignore-platform true
